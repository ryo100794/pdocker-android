# Shared Documents

Files placed here are mounted into the container at `/documents` by default.
Set `PDOCKER_DOCUMENTS_HOST` or `PDOCKER_DOCUMENTS_MOUNT` before compose up to
use a different host or container path.
