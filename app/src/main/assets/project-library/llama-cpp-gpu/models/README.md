Put GGUF models here.

Default path used by the compose template:

`models/model.gguf`

You can also set `LLAMA_MODEL_URL` before compose up to download a GGUF model
into the configured model path at startup.
